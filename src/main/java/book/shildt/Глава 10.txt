1. Байтовые потоки определены для чтений байтов, символьные потоки определены для чтения символов (но стоит сказать, что под капотом у обоих потоков
   используется чтение байтов).
2. Потому что изначально в Java были только байтовые потоки.
3. Необходимо создать объект класса FileInputStream
   FileInputStream fis = new FileInputStream("Путь к файлу");
4. Необходимо использовать объект класса
   FileReader fileReader = new FileReader("Путь к файлу");
5. 
6. Использовать метод parseDouble(String str) находящийся в классе Double
9. InputStream
10. Возвращает значение -1 
11. потоки байтового типа
12. символьных потоков
13. автоматического закрытия ресурсов
14. Да, верно. Потому что код для работы с ресурсом может выбросить исключение в блоке try до метода close() и таким образом закрытие ресурса не произойдет.